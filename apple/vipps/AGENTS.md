Keep provider access read-only and manual. Credentials live only in this app's Keychain service. Never log raw provider responses or secrets. Compare payouts to ReAI pending bank transactions; do not turn settlement totals into sales/VAT postings or silently confirm matches. Keep date ranges and pagination bounded, and show candidate matches as suggestions. Build with `python3 tools/apple/build.py <app>` from the repo root using Swift 6.4.
